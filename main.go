package main

// "fmt"

func main() {

	// addr, _ := net.ResolveUDPAddr("udp", "127.0.0.1:1900")
	// lc, _ := net.ListenUDP("udp", addr)
	// NewU

	// initial the Portal
	// lc, _ := net.ListenUDP("udp", nil)
	// p = &Portal{
	// 	UDPConn: lc,
	// }
	// go p.run()
	// go renewAddr(p, true)

}
